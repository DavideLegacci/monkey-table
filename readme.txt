MANUAL OPERATIONS DONE

- Set result column to text (avoid if at moment of exporting, set whole sheet to text)

- Make sure first column is parameters, not empty

AUTOMATIC

- Handles multiple exams on the same day, keeping first

- Automatically gets rid of kein material